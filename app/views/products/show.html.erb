<%= link_to 'Volver a Productos', products_path, class: 'btn btn-primary' %>
<h1><%= @product.name %></h1>
<img src="<%= @product.url %>" alt="<%= @product.name %>" class="img-fluid">
<p>Precio: $<%= @product.price %></p>